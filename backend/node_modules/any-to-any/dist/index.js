"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = exports.Convert = void 0;
const conversion_1 = require("./conversion");
const validation_1 = require("./validation");
function Convert(InputNumber, InputBase, OutputBase) {
    InputNumber = `${InputNumber}`.toUpperCase();
    validation_1.isValidInput(InputNumber, InputBase, OutputBase);
    return (InputBase === OutputBase ? InputNumber : ((() => {
        let signe = InputNumber.charAt(0) === '-' ? '-' : '';
        InputNumber = signe.length !== 0 ? InputNumber.substr(1, InputNumber.length) : InputNumber;
        return InputBase === 10 ? `${signe}${conversion_1.decimalToAnyBase(parseInt(InputNumber, 10), OutputBase)}` : (OutputBase === 10 ?
            `${signe}${conversion_1.anyBaseToDecimal(`${InputNumber}`, InputBase)}` :
            `${signe}${conversion_1.decimalToAnyBase(conversion_1.anyBaseToDecimal(`${InputNumber}`, InputBase), OutputBase)}`);
    })()));
}
exports.Convert = Convert;
exports.default = Convert;
;
// For CommonJS default export support 
module.exports = Convert;
module.exports.Convert = Convert;
module.exports.default = Convert;
